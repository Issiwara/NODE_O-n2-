name: Deploy to EC2

on:
  push:
    branches:
      - github_actions


jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: SSH into EC2 instance
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_PRIVATE_KEY }}
          port: ${{ secrets.EC2_PORT }}
          script: |
            sudo docker stop nodeOn2
            sudo docker rm nodeOn2
            sudo docker pull issiwara/node_on2
            sudo docker run -d -p 80:3000 --name nodeOn2 issiwara/node_on2
